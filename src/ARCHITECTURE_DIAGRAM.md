# 🏗️ Clinical Intelligence Engine - Architecture Diagram

## Storage Architecture (After Protection Implementation)

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         APPLICATION LAYER                                │
│  Components, Pages, UI Logic                                             │
└────────────────┬───────────────────────────────────────┬────────────────┘
                 │                                       │
                 │ Uses                                  │ Uses
                 ▼                                       ▼
┌────────────────────────────────────┐  ┌──────────────────────────────────┐
│      Custom Hooks Layer            │  │    Shared Types Layer            │
│  /components/*/hooks/              │  │    /types/shared.ts              │
│                                    │  │                                  │
│  • useDatabase()                   │  │  • SavedProtocol                 │
│  • useVersionControl()             │  │  • ProtocolVersion               │
│  • useProtocolLibrary()            │  │  • ClinicalDataRecord            │
│  • useSchemaState()                │  │  • DatabaseTable                 │
└────────────────┬───────────────────┘  │  • SchemaBlock                   │
                 │                      │  • SchemaTemplate                │
                 │ Calls                └──────────────────────────────────┘
                 ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                      STORAGE SERVICE LAYER                               │
│  /utils/storageService.ts                                                │
│                                                                           │
│  Provides type-safe, error-handled storage operations:                   │
│                                                                           │
│  storage.protocols.getAll()                                              │
│  storage.protocols.save()                                                │
│  storage.clinicalData.getAll()                                           │
│  storage.clinicalData.getByProtocol()                                    │
│  storage.templates.getAll()                                              │
│  storage.personas.getAll()                                               │
└────────────────┬────────────────────────────────────────────────────────┘
                 │
                 │ Uses
                 ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                      STORAGE KEYS REGISTRY                               │
│  /utils/storageKeys.ts                                                   │
│                                                                           │
│  STORAGE_KEYS.PROTOCOLS          → 'clinical-intelligence-protocols'    │
│  STORAGE_KEYS.CLINICAL_DATA      → 'clinical-intelligence-data'         │
│  STORAGE_KEYS.SCHEMA_TEMPLATES   → 'clinical-schema-templates'          │
│  STORAGE_KEYS.PERSONAS           → 'clinical-intelligence-personas'     │
└────────────────┬────────────────────────────────────────────────────────┘
                 │
                 │ Accesses
                 ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                      BROWSER LOCALSTORAGE                                │
│  window.localStorage                                                     │
│                                                                           │
│  Key: 'clinical-intelligence-protocols'                                  │
│  Value: [{ id: 'PROTO-001', versions: [...], ... }]                     │
│                                                                           │
│  Key: 'clinical-intelligence-data'                                       │
│  Value: [{ recordId: 'REC-001', data: {...}, ... }]                     │
│                                                                           │
│  Key: 'clinical-schema-templates'                                        │
│  Value: [{ id: 'TMPL-001', block: {...}, ... }]                         │
│                                                                           │
│  Key: 'clinical-intelligence-personas'                                   │
│  Value: [{ id: 'PERSONA-001', role: 'PI', ... }]                        │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## Module Architecture

```
┌───────────────────────────────────────────────────────────────────┐
│                    Clinical Intelligence Engine                    │
└───────────────────────────────────────────────────────────────────┘
                                 │
                ┌────────────────┼────────────────┐
                │                │                │
                ▼                ▼                ▼
┌──────────────────────┐ ┌──────────────┐ ┌─────────────────────┐
│  Protocol Workbench  │ │   Database   │ │  Protocol Library   │
│  /protocol-workbench/│ │  /database/  │ │ /protocol-library/  │
└──────────────────────┘ └──────────────┘ └─────────────────────┘
         │                       │                    │
         │                       │                    │
    ┌────┴────┐            ┌────┴────┐          ┌────┴────┐
    │         │            │         │          │         │
    ▼         ▼            ▼         ▼          ▼         ▼
┌───────┐ ┌────────┐  ┌───────┐ ┌───────┐  ┌───────┐ ┌───────┐
│ hooks │ │ comps  │  │ hooks │ │ comps │  │ hooks │ │ comps │
└───────┘ └────────┘  └───────┘ └───────┘  └───────┘ └───────┘
    │                      │                     │
    │                      │                     │
    └──────────────────────┼─────────────────────┘
                           │
                           ▼
              ┌────────────────────────┐
              │   Shared Utilities     │
              ├────────────────────────┤
              │ • /utils/storageService│
              │ • /utils/storageKeys   │
              │ • /utils/dataStorage   │
              │ • /types/shared        │
              └────────────────────────┘
```

---

## Data Flow: Protocol Creation → Database Use

```
┌──────────────────────────────────────────────────────────────────────┐
│  STEP 1: User Creates Protocol in Protocol Builder                   │
└──────────────────────────────────────────────────────────────────────┘
                                 │
                                 │ Protocol metadata + schema blocks
                                 ▼
┌──────────────────────────────────────────────────────────────────────┐
│  STEP 2: Save Protocol (useVersionControl hook)                      │
│                                                                       │
│  saveProtocol() → storage.protocols.save([...protocols])             │
└──────────────────────────────────────────────────────────────────────┘
                                 │
                                 │ Stored to localStorage
                                 │ Key: STORAGE_KEYS.PROTOCOLS
                                 ▼
┌──────────────────────────────────────────────────────────────────────┐
│  STORAGE: localStorage['clinical-intelligence-protocols']            │
│                                                                       │
│  [{                                                                   │
│    id: 'PROTO-001',                                                   │
│    protocolNumber: 'CIT-2026-001',                                    │
│    versions: [{                                                       │
│      id: 'v123',                                                      │
│      schemaBlocks: [...],                                             │
│      metadata: {...}                                                  │
│    }]                                                                 │
│  }]                                                                   │
└──────────────────────────────────────────────────────────────────────┘
                                 │
                                 │ Auto-refresh every 2s
                                 ▼
┌──────────────────────────────────────────────────────────────────────┐
│  STEP 3: Database Tab Loads Protocols (useDatabase hook)             │
│                                                                       │
│  const protocols = storage.protocols.getAll()                        │
│  Auto-select first protocol + version                                │
└──────────────────────────────────────────────────────────────────────┘
                                 │
                                 │ Selected protocol + version
                                 ▼
┌──────────────────────────────────────────────────────────────────────┐
│  STEP 4: Generate Database Tables (schemaGenerator)                  │
│                                                                       │
│  generateDatabaseTables(selectedVersion)                             │
│  → Converts schema blocks to database structure                      │
└──────────────────────────────────────────────────────────────────────┘
                                 │
                                 │ Database tables
                                 ▼
┌──────────────────────────────────────────────────────────────────────┐
│  STEP 5: Display in Database Views                                   │
│                                                                       │
│  • Schema View: Show table structure                                 │
│  • Data Entry: Dynamic form generation                               │
│  • Data Browser: Display collected data                              │
│  • Analytics: Analyze data                                           │
└──────────────────────────────────────────────────────────────────────┘
```

---

## Protection Layers

```
┌─────────────────────────────────────────────────────────────────────┐
│  Layer 5: Documentation & Guidelines                                 │
│  • ARCHITECTURE_PROTECTION.md                                        │
│  • QUICK_REFERENCE.md                                                │
│  • MIGRATION_GUIDE.md                                                │
│  Prevents: Developer confusion, inconsistent patterns               │
└─────────────────────────────────────────────────────────────────────┘
                                 ▲
┌─────────────────────────────────────────────────────────────────────┐
│  Layer 4: TypeScript Strict Mode                                    │
│  • Type checking at compile time                                     │
│  • Catches errors before runtime                                     │
│  Prevents: Type mismatches, undefined access                         │
└─────────────────────────────────────────────────────────────────────┘
                                 ▲
┌─────────────────────────────────────────────────────────────────────┐
│  Layer 3: Shared Types (/types/shared.ts)                           │
│  • Single source of truth for interfaces                             │
│  • Import instead of duplicate                                       │
│  Prevents: Type drift, interface inconsistencies                     │
└─────────────────────────────────────────────────────────────────────┘
                                 ▲
┌─────────────────────────────────────────────────────────────────────┐
│  Layer 2: Storage Keys Registry (/utils/storageKeys.ts)             │
│  • Centralized key definitions                                       │
│  • Type-safe key access                                              │
│  Prevents: Key typos, string literal mismatches                      │
└─────────────────────────────────────────────────────────────────────┘
                                 ▲
┌─────────────────────────────────────────────────────────────────────┐
│  Layer 1: Storage Service (/utils/storageService.ts)                │
│  • Error handling                                                    │
│  • Consistent API                                                    │
│  • Type safety                                                       │
│  Prevents: Unhandled errors, inconsistent access patterns           │
└─────────────────────────────────────────────────────────────────────┘
```

---

## Before vs After: localStorage Access

### ❌ BEFORE (Fragile)
```typescript
// In useDatabase.ts
const data = localStorage.getItem('clinicalProtocols'); // Wrong key!

// In useVersionControl.ts
const data = localStorage.getItem('clinical-intelligence-protocols'); // Different key!

// Result: Database can't find protocols! 🐛
```

### ✅ AFTER (Protected)
```typescript
// Both files use the same centralized key
import { STORAGE_KEYS } from '@/utils/storageKeys';

// In useDatabase.ts
const data = localStorage.getItem(STORAGE_KEYS.PROTOCOLS);

// In useVersionControl.ts
const data = localStorage.getItem(STORAGE_KEYS.PROTOCOLS);

// Result: Perfect sync! ✨
```

---

## Error Handling Flow

```
User Action (Save Data)
         │
         ▼
┌─────────────────────┐
│ Component           │
│ onClick={save}      │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────────────────┐
│ Custom Hook                     │
│ storage.protocols.save(data)    │
└──────────┬──────────────────────┘
           │
           ▼
┌──────────────────────────────────┐
│ Storage Service                  │
│ try {                            │
│   localStorage.setItem(...)      │
│   return true                    │──────┐
│ } catch (error) {                │      │ Success
│   console.error(...)             │      │
│   return false  ─────────────────┼──┐   │
│ }                                │  │   │
└──────────────────────────────────┘  │   │
                                      │   │
                                Fail  │   │  Success
                                      │   │
                                      ▼   ▼
                           ┌─────────────────────────┐
                           │ Component Handles       │
                           │ if (success) {          │
                           │   showSuccessToast()    │
                           │ } else {                │
                           │   showErrorToast()      │
                           │ }                       │
                           └─────────────────────────┘
```

---

## Key Concepts

### 🎯 Single Source of Truth
One place to define each thing:
- Storage keys → `/utils/storageKeys.ts`
- Types → `/types/shared.ts`
- Storage logic → `/utils/storageService.ts`

### 🔒 Type Safety
TypeScript ensures correctness at compile time

### 🛡️ Error Boundaries
Every storage operation wrapped in try-catch

### 📦 Modularity
Clear separation of concerns:
- Hooks manage state
- Components render UI
- Utils handle business logic

### 🔄 Consistency
Same patterns across entire codebase

---

## Quick Stats

```
Protection Level: ████████████████████ 100%

✅ Centralized Keys:      4/4 keys defined
✅ Type Definitions:      10+ shared types
✅ Error Handling:        All operations
✅ Documentation:         4 comprehensive guides
✅ Migration Path:        Non-breaking, gradual
✅ Developer Experience:  Excellent (auto-complete, type-safe)
```

---

**Use this diagram to understand the system architecture at a glance.**

For detailed information:
- Full Guide: `/ARCHITECTURE_PROTECTION.md`
- Quick Reference: `/QUICK_REFERENCE.md`
- Migration: `/MIGRATION_GUIDE.md`
